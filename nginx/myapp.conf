server {
	listen 80;
	server_name _;
	root /var/www/nginxapp/frontend;
	index index.html;

	location / {
		try_files $uri $uri/ =404;
	}
	location /api/ {
		proxy_pass http://3.235.179.90:5000/;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_connect_timeout 60s;
	}
	location /static {
		alias /var/www/nginxapp/frontend;
	}
}

